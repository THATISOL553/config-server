eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    
spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Cva@@51235213
    url: jdbc:mysql://localhost:3306/BATCH25
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
springdoc:
  swagger-ui:
    path: swagger-ui.html
